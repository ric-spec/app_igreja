[theme]
base = "dark"
primaryColor = "#60a5fa"
backgroundColor = "#0f172a"
secondaryBackgroundColor = "#111827"
textColor = "#e2e8f0"
font = "sans serif"

[server]
headless = true
# O Streamlit Cloud fornece a porta automaticamente.
# Não defina um valor de porta fixo para evitar falhas de healthcheck.
